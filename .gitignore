# OS
.DS_Store
Thumbs.db

# IDEs
.vscode/
.idea/

# Node
node_modules/

# Secrets
.env

# Build output (shareable single-file deck)
dist/

# Local backup written by server.js on each save
cards.json.bak

# Non-destructive art slots: pending candidate (.new) + one-deep undo (.prev).
# Only the chosen canonical art/<id>.png is committed; alternates stay local.
public/art/*.new.*
public/art/*.prev.*

# Claude Code
.claude/*
!.claude/skills/
